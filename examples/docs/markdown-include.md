# Markdown Include

Shared Markdown section via short ref:

```markdown-include
customer-verification.section
```

Shared Markdown section via explicit path ref:

```md-include
../shared/markdown-sections.md#customer-verification.alias-section
```
